<UserControl x:Class="com.example.Controls.Wpf.Display.ModernCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="320" d:DesignHeight="160">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/com.example;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- 둥근 테두리 카드. 제목/부제목(선택) + 내용(InnerContent)을 담습니다. -->
    <Border CornerRadius="{StaticResource Radius.Lg}"
            Background="{StaticResource Brush.Surface}"
            BorderBrush="{StaticResource Brush.BorderSubtle}"
            BorderThickness="1"
            Padding="16">
        <StackPanel>
            <TextBlock x:Name="TitleText"
                       Style="{StaticResource Text.Title}"
                       Text="{Binding Title, ElementName=RootControl}" />
            <TextBlock x:Name="SubtitleText"
                       Style="{StaticResource Text.Helper}"
                       Margin="0,2,0,0"
                       Text="{Binding Subtitle, ElementName=RootControl}" />
            <ContentPresenter x:Name="BodyPresenter"
                              Margin="0,12,0,0"
                              Content="{Binding InnerContent, ElementName=RootControl}" />
        </StackPanel>
    </Border>
</UserControl>
