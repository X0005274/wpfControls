<UserControl x:Class="com.example.Controls.Wpf.Display.ModernDividerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="320">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/com.example;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- 가로 구분선. 가운데 글자가 있으면 선 사이에 글자를 표시합니다. -->
    <Grid Margin="0,8">
        <Border Height="1" Background="{StaticResource Brush.BorderSubtle}" VerticalAlignment="Center" />
        <Border x:Name="TextHolder"
                Background="{StaticResource Brush.Background}"
                Padding="8,0"
                HorizontalAlignment="Center">
            <TextBlock x:Name="LabelText"
                       Text="{Binding Text, ElementName=RootControl}"
                       FontSize="{StaticResource Font.Size.Helper}"
                       Foreground="{StaticResource Brush.TextSecondary}" />
        </Border>
    </Grid>
</UserControl>
