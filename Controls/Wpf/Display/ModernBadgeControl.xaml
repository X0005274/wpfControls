<UserControl x:Class="com.example.Controls.Wpf.Display.ModernBadgeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="RootControl"
             FontFamily="pack://application:,,,/com.example;component/Assets/Fonts/#Noto Sans KR, Segoe UI, Malgun Gothic"
             HorizontalAlignment="Left"
             VerticalAlignment="Center">
    <Border x:Name="BadgeBorder"
            CornerRadius="10"
            Background="#FFEFF6FF"
            Padding="10,3"
            HorizontalAlignment="Left">
        <TextBlock x:Name="BadgeTextBlock"
                   Text="{Binding BadgeText, ElementName=RootControl}"
                   FontSize="11"
                   FontWeight="SemiBold"
                   Foreground="#FF1D4ED8"
                   VerticalAlignment="Center" />
    </Border>

</UserControl>
