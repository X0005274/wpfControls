<UserControl x:Class="com.example.Controls.Wpf.Layout.ModernPageHeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="480" d:DesignHeight="64">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/com.example;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- 화면 상단 머리글: 왼쪽 제목/부제목, 오른쪽 액션 영역(버튼 등) -->
    <Grid Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock Text="{Binding Title, ElementName=RootControl}"
                       FontSize="{StaticResource Font.Size.Heading}"
                       FontWeight="Medium"
                       Foreground="{StaticResource Brush.TextPrimary}" />
            <TextBlock x:Name="SubtitleText"
                       Style="{StaticResource Text.Helper}"
                       Margin="0,2,0,0"
                       Text="{Binding Subtitle, ElementName=RootControl}" />
        </StackPanel>

        <!-- 오른쪽 액션 영역: 버튼 등을 코드에서 InnerContent로 넣습니다. -->
        <ContentPresenter Grid.Column="1"
                          VerticalAlignment="Center"
                          Content="{Binding InnerContent, ElementName=RootControl}" />
    </Grid>
</UserControl>
