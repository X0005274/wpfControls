<UserControl x:Class="com.example.Controls.Wpf.Layout.ModernScrollSectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="360" d:DesignHeight="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/com.example;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border CornerRadius="{StaticResource Radius.Lg}"
            BorderThickness="1"
            BorderBrush="{StaticResource Brush.BorderSubtle}"
            Background="{StaticResource Brush.Surface}"
            ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Fixed header -->
            <Border Grid.Row="0"
                    Background="{StaticResource Brush.HeaderBackground}"
                    BorderBrush="{StaticResource Brush.BorderSubtle}"
                    BorderThickness="0,0,0,1"
                    Padding="14,12">
                <TextBlock Style="{StaticResource Text.Title}"
                           FontSize="13"
                           Text="{Binding HeaderText, ElementName=RootControl}" />
            </Border>

            <!-- Scrollable content -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="16,12">
                <ContentPresenter Content="{Binding InnerContent, ElementName=RootControl}" />
            </ScrollViewer>
        </Grid>
    </Border>

</UserControl>
