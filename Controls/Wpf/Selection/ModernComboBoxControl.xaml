<UserControl x:Class="com.example.Controls.Wpf.Selection.ModernComboBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="320">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/com.example;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

            <!-- Dropdown item: hover / selected from tokens, with a check affordance -->
            <Style x:Key="ModernComboBoxItemStyle" TargetType="ComboBoxItem">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="ItemBorder"
                                    Background="Transparent"
                                    CornerRadius="{StaticResource Radius.Md}"
                                    Margin="4,1"
                                    Padding="{StaticResource Pad.Item}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" VerticalAlignment="Center" />
                                    <Path x:Name="CheckMark"
                                          Grid.Column="1"
                                          Width="12" Height="9"
                                          Margin="10,0,0,0"
                                          Stretch="Uniform"
                                          Stroke="{StaticResource Brush.Accent}"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Data="M1,5 L4.5,8.5 L11,1"
                                          VerticalAlignment="Center"
                                          Visibility="Collapsed" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="IsSelected" Value="False" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource Brush.HoverBackground}" />
                                </MultiTrigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource Brush.SelectedBackground}" />
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                                    <Setter Property="Foreground" Value="{StaticResource Brush.SelectedText}" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Transparent click surface for the chevron area -->
            <ControlTemplate x:Key="ChevronToggleTemplate" TargetType="ToggleButton">
                <Border Background="Transparent" />
            </ControlTemplate>

            <!-- Full ComboBox template, token-driven -->
            <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                <Setter Property="MinHeight" Value="{StaticResource Size.ControlHeight}" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="ItemContainerStyle" Value="{StaticResource ModernComboBoxItemStyle}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <!-- Outer focus ring (flat, no shadow) -->
                                <Border x:Name="FocusRing"
                                        CornerRadius="{StaticResource Radius.Md}"
                                        BorderThickness="1"
                                        BorderBrush="Transparent">
                                    <Border x:Name="FieldBorder"
                                            CornerRadius="{StaticResource Radius.Md}"
                                            BorderThickness="1"
                                            BorderBrush="{StaticResource Brush.Border}"
                                            Background="{StaticResource Brush.Surface}"
                                            SnapsToDevicePixels="True">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="38" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Input area -->
                                            <Grid Grid.Column="0" Margin="12,0,2,0">
                                                <ContentPresenter x:Name="ContentSite"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"
                                                                  IsHitTestVisible="False"
                                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />
                                                <!-- PART_EditableTextBox is required for editable mode. -->
                                                <TextBox x:Name="PART_EditableTextBox"
                                                         VerticalAlignment="Center"
                                                         VerticalContentAlignment="Center"
                                                         Background="Transparent"
                                                         BorderThickness="0"
                                                         Padding="0"
                                                         FontSize="{StaticResource Font.Size.Body}"
                                                         Foreground="{StaticResource Brush.TextPrimary}"
                                                         CaretBrush="{StaticResource Brush.TextPrimary}"
                                                         SelectionBrush="{StaticResource Brush.Accent}"
                                                         SelectionOpacity="0.22"
                                                         Focusable="True"
                                                         IsReadOnly="{TemplateBinding IsReadOnly}"
                                                         Visibility="Hidden" />
                                            </Grid>

                                            <!-- Chevron area: divider + chevron + transparent toggle -->
                                            <Grid Grid.Column="1">
                                                <Rectangle Width="1"
                                                           HorizontalAlignment="Left"
                                                           Fill="{StaticResource Brush.BorderSubtle}"
                                                           Margin="0,9" />
                                                <Path x:Name="Arrow"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Width="10" Height="6"
                                                      Stretch="Uniform"
                                                      Stroke="{StaticResource Brush.TextSecondary}"
                                                      StrokeThickness="1.6"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      StrokeLineJoin="Round"
                                                      Data="M1,1 L5,5 L9,1"
                                                      RenderTransformOrigin="0.5,0.5">
                                                    <Path.RenderTransform>
                                                        <RotateTransform Angle="0" />
                                                    </Path.RenderTransform>
                                                </Path>
                                                <ToggleButton x:Name="ToggleButton"
                                                              Focusable="False"
                                                              ClickMode="Press"
                                                              Template="{StaticResource ChevronToggleTemplate}"
                                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </Border>

                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Fade">
                                    <Border x:Name="DropDownBorder"
                                            MinWidth="{Binding ActualWidth, ElementName=FieldBorder}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                            Margin="0,6,0,8"
                                            CornerRadius="{StaticResource Radius.Lg}"
                                            BorderThickness="1"
                                            BorderBrush="{StaticResource Brush.BorderSubtle}"
                                            Background="{StaticResource Brush.Surface}"
                                            SnapsToDevicePixels="True"
                                            Effect="{StaticResource Shadow.Popup}">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,4">
                                            <ItemsPresenter SnapsToDevicePixels="True" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEditable" Value="True">
                                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource Brush.BorderHover}" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource Brush.FocusBorder}" />
                                    <Setter TargetName="FocusRing" Property="BorderBrush" Value="{StaticResource Brush.FocusRing}" />
                                </Trigger>
                                <Trigger Property="IsDropDownOpen" Value="True">
                                    <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource Brush.FocusBorder}" />
                                    <Setter TargetName="FocusRing" Property="BorderBrush" Value="{StaticResource Brush.FocusRing}" />
                                    <Setter TargetName="Arrow" Property="Stroke" Value="{StaticResource Brush.Accent}" />
                                    <Setter TargetName="Arrow" Property="RenderTransform">
                                        <Setter.Value>
                                            <RotateTransform Angle="180" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="FieldBorder" Property="Background" Value="{StaticResource Brush.DisabledBackground}" />
                                    <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource Brush.BorderSubtle}" />
                                    <Setter TargetName="Arrow" Property="Stroke" Value="{StaticResource Brush.DisabledText}" />
                                    <Setter TargetName="PART_EditableTextBox" Property="Foreground" Value="{StaticResource Brush.DisabledText}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="{StaticResource Gap.Label}">
            <TextBlock Style="{StaticResource Text.Label}"
                       Text="{Binding Title, ElementName=RootControl}" />
            <TextBlock Text="*"
                       FontFamily="{DynamicResource Font.Family}"
                       FontSize="{StaticResource Font.Size.Label}"
                       FontWeight="Medium"
                       Foreground="{StaticResource Brush.ErrorBorder}"
                       Margin="3,0,0,0"
                       Visibility="{Binding IsRequired, ElementName=RootControl, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>

        <ComboBox x:Name="InnerComboBox"
                  Style="{StaticResource ModernComboBoxStyle}"
                  IsEditable="True"
                  IsTextSearchEnabled="False"
                  StaysOpenOnEdit="True"
                  IsSynchronizedWithCurrentItem="False"
                  ItemsSource="{Binding ItemsSource, ElementName=RootControl}"
                  SelectedItem="{Binding SelectedItem, ElementName=RootControl, Mode=TwoWay}"
                  SelectedValue="{Binding SelectedValue, ElementName=RootControl, Mode=TwoWay}"
                  DisplayMemberPath="{Binding DisplayMemberPath, ElementName=RootControl}"
                  SelectedValuePath="{Binding SelectedValuePath, ElementName=RootControl}"
                  IsEnabled="{Binding IsEditorEnabled, ElementName=RootControl}" />
    </StackPanel>

</UserControl>
