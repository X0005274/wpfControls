<UserControl x:Class="com.example.Controls.Wpf.Input.ModernFilePickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="360">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/com.example;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="{StaticResource Gap.Label}">
            <TextBlock x:Name="TitleText"
                       Style="{StaticResource Text.Label}"
                       Text="{Binding Title, ElementName=RootControl}" />
            <TextBlock x:Name="RequiredMark"
                       Text="*"
                       FontFamily="{DynamicResource Font.Family}"
                       FontSize="{StaticResource Font.Size.Label}" FontWeight="Medium"
                       Foreground="{StaticResource Brush.ErrorBorder}"
                       Margin="3,0,0,0" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- 선택된 파일 경로(읽기 전용 표시) -->
            <Border Grid.Column="0"
                    BorderBrush="{StaticResource Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius.Md}"
                    Background="{StaticResource Brush.SurfaceAlt}"
                    Height="{StaticResource Size.ControlHeight}">
                <Grid>
                    <TextBlock x:Name="Placeholder"
                               Text="{Binding PlaceholderText, ElementName=RootControl}"
                               Foreground="{StaticResource Brush.TextPlaceholder}"
                               FontSize="{StaticResource Font.Size.Body}" IsHitTestVisible="False"
                               VerticalAlignment="Center" Margin="12,0" />
                    <TextBox x:Name="PathTextBox"
                             IsReadOnly="True"
                             VerticalContentAlignment="Center"
                             Padding="12,0"
                             BorderThickness="0"
                             Background="Transparent"
                             FontSize="{StaticResource Font.Size.Body}"
                             Foreground="{StaticResource Brush.TextPrimary}"
                             Text="{Binding FilePath, ElementName=RootControl, Mode=OneWay}" />
                </Grid>
            </Border>

            <!-- 찾아보기 버튼 -->
            <Button x:Name="BrowseButton" Grid.Column="1"
                    Content="찾아보기"
                    Margin="8,0,0,0"
                    Padding="14,0"
                    Height="{StaticResource Size.ControlHeight}"
                    FontSize="{StaticResource Font.Size.Body}"
                    Cursor="Hand"
                    Background="{StaticResource Brush.Surface}"
                    Foreground="{StaticResource Brush.TextPrimary}"
                    BorderBrush="{StaticResource Brush.Border}"
                    BorderThickness="1"
                    IsEnabled="{Binding IsEditorEnabled, ElementName=RootControl}"
                    Click="BrowseButton_Click" />
        </Grid>
    </StackPanel>
</UserControl>
